import { Meta } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'

<Meta title="ivt/components/Sortable" />

# Sortable

Use this component for drag and drop functionality, it uses [SortableJS](https://sortablejs.github.io/Sortable/) under the hood.
